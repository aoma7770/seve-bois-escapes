# Development placeholders — overridden by /run/base44/app.env (dashboard secrets).
# These exist so the app boots without external credentials; integrations stay non-functional
# until the user supplies real values via the Base44 dashboard.

JWT_SECRET=development-only-jwt-secret-placeholder
VITE_APP_ID=dev
VITE_OAUTH_PORTAL_URL=http://localhost:3000
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=support@sevebois.be
SMTP_PASS=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
